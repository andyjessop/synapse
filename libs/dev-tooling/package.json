{
  "name": "dev-tooling",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "biome check .",
    "typecheck": "tsc --noEmit -p ../../tsconfig.json",
    "test": "vitest run --coverage",
    "format": "biome format --write ."
  }
}
